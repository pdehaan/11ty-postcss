<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <style>
    {%- postcss -%}
      {% include entry.css %}
    {%- endpostcss -%}
  </style>
  <title>{{ title }}</title>
</head>
<body>

  <h1>{{ title }}</h1>
  <div class="container">{{ content }}</div>

</body>
</html>
